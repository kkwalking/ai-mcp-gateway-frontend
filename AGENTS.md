# Repository Guidelines

## 分支开发规则
前端仓库开发新需求、修复缺陷、调整规则或补充文档时，都必须先从 `main` 新建 `codex/` 前缀分支进行开发。禁止直接在 `main` 分支编辑或提交代码。

完成开发后，先在开发分支提交，再切回 `main` 通过 `merge` 合并；需要保留合并记录时优先使用 `--no-ff`。涉及后端联动时，后端仓库 `~/code/ai-mcp-gateway` 也要分别遵守该流程。

## 相关项目
当前前端项目对应的后端项目目录是 `~/code/ai-mcp-gateway`。涉及接口契约、后端行为、联调或跨端类型变更时，默认到该目录查看或修改后端代码，无需用户重复说明。

